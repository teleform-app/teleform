import styled from 'styled-components'

export const FormShareWrapper = styled.div`
    font-family: var(--font-regular);
    font-size: 14px;
    font-style: normal;
    font-weight: 500;

    color: var(--accent);
`
